import 'package:flutter/widgets.dart';

/// Stub (web / plateformes sans dart:io) : pas d'accès fichier local.
Widget? localFileImage(
  String? path, {
  BoxFit fit = BoxFit.cover,
  double? width,
  double? height,
}) =>
    null;
