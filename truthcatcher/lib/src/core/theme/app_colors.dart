import 'package:flutter/material.dart';

/// Palette TruthCatcher (reprend les teintes de l'app d'origine).
class AppColors {
  const AppColors._();

  static const Color primary = Color(0xFF2D64E3);
  static const Color background = Color(0xFFEEEAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color success = Color(0xFF2BB673);
  static const Color danger = Color(0xFFE3452D);
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textMuted = Color(0xFF6B6B80);
}
