# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock        # app -> on ne committe pas le lock (à ajuster selon préférence)

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Secrets / env
.env
*.keystore
*.jks
google-services.json
GoogleService-Info.plist

# Générés (plateformes) — recréés par `flutter create .`
# (on garde lib/ et la config ; les dossiers natifs sont régénérables)
